<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:TataApp.Views;assembly=TataApp"
             x:Class="TataApp.Views.MasterPage">
    <MasterDetailPage.Master>
        <pages:MenuPage>
        </pages:MenuPage>
    </MasterDetailPage.Master>
    <MasterDetailPage.Detail>
        <NavigationPage
            BarTextColor="{StaticResource BackgroundColor}"
            BarBackgroundColor="{StaticResource MainColor}"
            x:Name="Navigator">
            <x:Arguments>
                <pages:HomePage>
                </pages:HomePage>
            </x:Arguments>
        </NavigationPage>
    </MasterDetailPage.Detail>
</MasterDetailPage>